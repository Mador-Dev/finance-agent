# GSD Documentation Archive

This directory contains GSD planning and execution documentation that should travel with the repository.

## Current planning package

- [Postgres-Owned Product State Migration](postgres-owned-product-state-migration/README.md) — the active documentation package for the next implementation phase. This is the package to give another model or another machine before implementing the milestone.

## Historical archive

- `.gsd/projects/7f83221b3b0b/` — preserved historical GSD project archive from an earlier pilot-confidence milestone. It was moved here wholesale from the repository root so `gsd-docs` lives under `docs/`.

The hidden archive is retained for traceability, but the current milestone should be read from the clean package above, not by mining old JSONL logs.
